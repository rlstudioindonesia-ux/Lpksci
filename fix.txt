                return { ...entry, saldo: runningSaldo };
              });
              const knownCodes = ["P1", "P2", "P3", "P4", "P5", "P6", "P7", "P8", "P9A", "P9B"];
              const filteredLedger = ledgerWithSaldo.filter(entry => {
                const code = (entry.code || "DLL").toUpperCase();
                const matchesCategory = ledgerCategoryFilter === "ALL" || code === ledgerCategoryFilter.toUpperCase() || (ledgerCategoryFilter === "DLL" const filteredLedger = ledgerWithSaldo.filter(entry => (ledgerCategoryFilter === "ALL" || (entry.code || "DLL").toUpperCase() === ledgerCategoryFilter.toUpperCase()) && (entry.description.toLowerCase().includes(ledgerSearch.toLowerCase()) || (entry.code || "").toLowerCase().includes(ledgerSearch.toLowerCase())));const filteredLedger = ledgerWithSaldo.filter(entry => (ledgerCategoryFilter === "ALL" || (entry.code || "DLL").toUpperCase() === ledgerCategoryFilter.toUpperCase()) && (entry.description.toLowerCase().includes(ledgerSearch.toLowerCase()) || (entry.code || "").toLowerCase().includes(ledgerSearch.toLowerCase()))); !knownCodes.includes(code));
                return matchesCategory const filteredLedger = ledgerWithSaldo.filter(entry => (ledgerCategoryFilter === "ALL" || (entry.code || "DLL").toUpperCase() === ledgerCategoryFilter.toUpperCase()) && (entry.description.toLowerCase().includes(ledgerSearch.toLowerCase()) || (entry.code || "").toLowerCase().includes(ledgerSearch.toLowerCase())));const filteredLedger = ledgerWithSaldo.filter(entry => (ledgerCategoryFilter === "ALL" || (entry.code || "DLL").toUpperCase() === ledgerCategoryFilter.toUpperCase()) && (entry.description.toLowerCase().includes(ledgerSearch.toLowerCase()) || (entry.code || "").toLowerCase().includes(ledgerSearch.toLowerCase()))); (entry.description.toLowerCase().includes(ledgerSearch.toLowerCase()) || code.toLowerCase().includes(ledgerSearch.toLowerCase()));
              });
              const itemsPerPage = 10;
